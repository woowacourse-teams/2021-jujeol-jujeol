spring:
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
  profiles:
    group:
      "local": "console-logging"
      "dev" : "file-logging"
      "prod" : "file-logging"
